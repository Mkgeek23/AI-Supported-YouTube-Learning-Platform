In this lesson, you'll work with AI-generated tests for the application. Before getting started, complete the "AI in QA Engineering" course.

The codebase now contains all project files and AI-generated tests in one folder. While this results in some duplication, it's necessary for two reasons:

1. To ask Junie to write tests with full coverage based on the whole project.  
2. In the next lesson, you'll deploy the app from one directory to a production server.

The tests were generated by Junie using the following prompts:

```
**Prompt 1:** Look at the existing project of YouTube Learning Platform in `GenTests` directory.
I want you to write full coverage tests for Python code using `unittest` package.
Suggest step-by-step execution plan for this project. Write the plan to `docs/plan.md` file.
```

```
**Prompt 2 (follow up):** Start implementing all the tests and add the suffix `_junie`
to the names of generated files. Follow the `docs/plan.md` file and mark the tasks as done [x] upon completion.
```

## **Task**
This time, your task is a bit different: fix all bugs in the agent-generated code 
in [test_modules_junie.py](file://AIinQA/GenTests/test_modules_junie.py), 
[test_quizzes_junie.py](file://AIinQA/GenTests/test_quizzes_junie.py),
[test_main_junie.py](file://AIinQA/GenTests/test_main_junie.py),
and [test_e2e_junie.py](file://AIinQA/GenTests/test_e2e_junie.py).
