Now, you'll work on functionality that transforms YouTube educational videos into structured course modules.

## **API endpoints**

* `GET /`: Serves the main application page.  
* (new) `GET /modules`: Accepts a video ID and returns structured modules.

## **Task**

Complete the code in `task.py` and check the result.
